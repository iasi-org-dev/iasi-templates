# Manuales

Este directorio agrupa las publicaciones del multiproyecto `iasi-docs`.

Las publicaciones se organizan por componente:

```text
core/
cli/
agent/
```

Cada publicación es un libro IASI Quarto independiente con estrategia `regular`.
